# Paths are anchored to the repository root. Wildcard patterns are not allowed
# in this file under automatic analysis, so every path below is literal.

# Each example must stand alone, so that a reader can copy one directory and
# run it. The Next.js suites therefore repeat the same build, serve and assert
# flow against different Next.js majors on purpose.
sonar.cpd.exclusions=example/next/src/index.test.js,example/next15/src/index.test.js
